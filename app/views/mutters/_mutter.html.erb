<div class="mutter_content" id="mutter<%= mutter.id %>">
  <div class="icon">
    <p>
    <%= image_tag mutter.user_image_path, :size => "50x50", alt: "プロフィール画像" %>
    </p>
  </div>
  <div class="content-area">
  <% if request.smart_phone? %>
    <div class="info-area smp">
      <%= link_to mutter.user.dispname, mutter_by_user_path(mutter.user.id), {:title => mutter.user.dispname(User::FULLNAME)} if mutter.user.present? %><br>
      <%= link_to (l mutter.created_at), mutter_path(mutter, anchor: "mutter#{mutter.id}") %><br>
      <span class="nice">
        <span id='cont_<%= mutter.id %>'>
          <%= nice_field(mutter, "mutter", "#cont_#{mutter.id}") %>
        </span>
        <% # 検索時などはmutterが子である場合もあるので要判定 -%>
        <% if mutter.parent? -%>
          <a data-toggle="modal" href="#myModal<%= mutter.id %>" ><%= fa_icon 'comment', text: 'レス' %></a>
      <% end -%>
      </span>
    </div> <!-- info-area -->

  <% else %>
    <div class="info-area">
      <div class="info-left">
        <span class='name'><%= link_to mutter.user.dispname, mutter_by_user_path(mutter.user.id), {:title => mutter.user.dispname(User::FULLNAME)} if mutter.user.present? %></span>
        <span class='ml5'><%= useragent_short(mutter.ua) %></span>
        <span class='time'><%= link_to (l mutter.created_at), mutter_path(mutter, anchor: "mutter#{mutter.id}") %></span>
      </div>
      <div class="info-right">
        <span class="nice">
          <span id='cont_<%= mutter.id %>'>
            <%= nice_field(mutter, "mutter", "#cont_#{mutter.id}") %>
          </span>
          <% # 検索時などはmutterが子である場合もあるので要判定 -%>
          <% if mutter.parent? -%>
            <a data-toggle="modal" href="#myModal<%= mutter.id %>" ><%= fa_icon 'comment', text: 'レス' %></a>
        <% end -%>
        </span>
      </div>
    </div> <!-- info-area -->
  <% end %>

    <div class="content">
      <%== draw_content(mutter) %>
    </div>
    <div class='delete' data-mutter-id="<%= mutter.user&.id %>" style="display:none">
      <p><%= link_to("削除", mutter, data: {confirm: "本当に削除してもよろしいですか？"}, :method => :delete, remote: true) %></p>
    </div>
  </div>
</div>

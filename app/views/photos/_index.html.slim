- if photos.present?
  div[class='#{browser.device.mobile? ? 'smartphone' : ''}']
    .cards-narrow.card-columns
      = render partial: 'photos/boxed_photo', collection: photos, as: :photo, locals: { album: album }


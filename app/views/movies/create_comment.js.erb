$('#comment_form #content').val('');
$('#comments').html(
  '<%= escape_javascript(render partial: "shared/comments", locals: { model_name: 'Movie', comments: @movie.mutter_comments, movie: @movie }) %>'
);

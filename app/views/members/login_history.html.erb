<div class="row">
  <div class="span7 offset2">

<div class="form-actions">
<%= link_to "親戚データ一覧へ", members_path, class: "btn" %>
</div>

<h1>（ログインするタイミングってそのユーザーの活動を表すものにはなりえないので、この機能は廃止状態）</h1>

<%= paginate @lhs, window: 2 %>
<table class="table table-striped table-bordered table-hover table-condensed">
  <tr><th>LoginHistory ID</th><th>time</th><th>name</th><th>ログイン回数</th><tr>
<% @lhs.each do |lh| -%>
  <tr>
    <td><%= lh.id %></td>
    <td><%= lh.created_at.strftime("%Y/%m/%d %H:%M:%S") %></td>
    <td><%= lh.user.username if lh.user %></td>
    <td><%= lh.user.sign_in_count if lh.user %></td>
  </tr>
<% end -%>
</table>
<%= paginate @lhs, window: 2 %>

  </div>
</div>

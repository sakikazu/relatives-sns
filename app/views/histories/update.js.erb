<% if @history.errors.any? %>
  $('#error_message').show();
  <% @history.errors.full_messages.each do |msg| %>
    $('#error_message > ul').append("<li><%= msg %></li>");
  <% end %>

<% else %>
  $('#history_data').html(
    '<%= escape_javascript(render "data") %>'
  );
  $('#info')
    .show()
    .html('更新しました');
  $(".colorbox").colorbox();
<% end %>

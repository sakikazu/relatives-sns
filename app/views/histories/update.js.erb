<% if @history.errors.any? %>
  $('#info')
    .show()
    .html(
      '<%= escape_javascript(render "error") %>'
    )
<% else %>
  $('#history_data').html(
    '<%= escape_javascript(render "data") %>'
  );
  $('#info')
    .show()
    .html('更新しました');
<% end %>

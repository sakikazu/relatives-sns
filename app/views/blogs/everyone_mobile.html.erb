  <% @blog_count_by_user.each do |user, cnt, created_at| -%>
    <%= link_to "#{user.dispname}(#{cnt})", :action => :index_mobile, :username => user.username %><br />
    最終更新日：<%= created_at.strftime("%Y/%m/%d %H:%M:%S") %><br />
  <% end -%>

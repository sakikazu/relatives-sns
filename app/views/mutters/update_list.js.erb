<% if @error_message.present? %>
    alert("<%= @error_message %>");
<% else %>
$('#mutter_data').html(
  '<%= escape_javascript(render :partial => "list", :locals => {:mutters => @mutters}) %>'
);
// turbolinksによる遷移じゃない時は、これまで通り、DOM構築イベントでやる必要がある
$(function() {
  showMutterDeleteLink();
});
<% end %>
